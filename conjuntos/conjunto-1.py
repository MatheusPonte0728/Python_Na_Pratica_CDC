# Conjuntos: elementos não ordenados (sem posição) e não podem se repetir.
# representados por chaves "{}"

conjunto = {10, 8, 2, 3, 8, 10}
print(f"Tipo do conjunto: {type(conjunto)}")

print(conjunto)
